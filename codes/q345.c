#include <stdio.h>
main()
{	    		  	  
    int a,b,max;
    printf("input a and b=");
    scanf("%d%d",&a,&b);
    if(a>b) max=a;
    else max=b;
    printf("The max of a and b :%d\n",max);
}	    		  	  
